import React from 'react';

import Header from '../Header';
import Intro from '../Intro';

const Layout = () => {
  return (
    <React.Fragment>
      <Header />
      <Intro />
    </React.Fragment>
  );
};

export default Layout;
